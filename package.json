{
  "name": "obsidian-txt-exporter",
  "version": "0.1.0",
  "description": "Obsidian plugin: export notes to TXT",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "obsidian": "^1.6.0",
    "typescript": "^5.3.0"
  }
}
